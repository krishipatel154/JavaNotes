public class AccessSpecifier {
  public static void main(String[] args) {
    System.out.println("Access Specifier");

    // 1) public - outside the class, program, package
    // 2) protected - outside the class, program, package(using inheritance)
    // 3) default - outside the class, program but in only same package
    // 4) private - inside the class only
  }
}
